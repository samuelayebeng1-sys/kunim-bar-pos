kind = "web"
previewPath = "/"
title = "Kunim Bar POS"
version = "1.0.0"
id = "artifacts/kunim-pos"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23362

[services.development]
run = "pnpm --filter @workspace/kunim-pos run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/kunim-pos", "run", "build" ]
publicDir = "artifacts/kunim-pos/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23362"
BASE_PATH = "/"
